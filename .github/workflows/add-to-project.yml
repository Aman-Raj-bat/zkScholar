name: Add to Project
on:
  issues: { types: [opened] }
  pull_request: { types: [opened] }
permissions: { contents: read }
jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: Triage
        run: echo "Triaging new issue/PR"
